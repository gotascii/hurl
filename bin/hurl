#!/usr/bin/env ruby

require File.expand_path(
    File.join(File.dirname(__FILE__), '..', 'lib', 'hurl'))

include Hurl
project_dir = ARGV.shift
using ITerm
["", "lib"].each do |path|
  hurls = File.join(project_dir, 'path', 'hurls.rb')
  require hurls if File.exist? hurls
end

